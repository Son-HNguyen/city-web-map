import { GlobeDirective } from './globe.directive';

describe('GlobeDirective', () => {
  it('should create an instance', () => {
    const directive = new GlobeDirective();
    expect(directive).toBeTruthy();
  });
});
